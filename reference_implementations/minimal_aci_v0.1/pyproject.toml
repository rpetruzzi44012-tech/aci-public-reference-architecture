[project]
name = "aci-prototype"
version = "0.1.0"
description = "Minimal category-collapse test harness for Architectures of Coherent Intelligence"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
